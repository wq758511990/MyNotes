/**
 * @param {number[]} nums1
 * @param {number[]} nums2
 * @return {number}
 */
 var findMedianSortedArrays = function(nums1, nums2) {
    // const ans = nums1.concat(nums2).sort((a, b) => a - b)
    // const n = ans.length
    // return n % 2 === 0 ? (ans[(n / 2) - 1] + ans[(n / 2)]) / 2 : ans[Math.floor(n / 2)]
};

console.log(findMedianSortedArrays([1, 3], [2]))