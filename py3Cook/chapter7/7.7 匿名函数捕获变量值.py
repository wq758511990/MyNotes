x = 10
a = lambda y: x + y
# a = lambda y, x = x: x + y  # 这样就可以在定义时就捕获到值，
x = 20
print(a(10)) # 这里输出的是30而非20， 

# 由于 lambda 表达式中的 x 是一个自由变量，在运行时绑定值，而不
# 是定义时就绑定，这跟函数的默认值参数定义是不同的。因此，在调用这个 lambda 表
# 达式的时候，x 的值是执行时的值。

